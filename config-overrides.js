// This file is required by react-app-rewired
// It can be used to customize the webpack config without ejecting

module.exports = function override(config, env) {
    // Add any webpack config overrides here
    return config;
} 